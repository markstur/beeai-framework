[tools]
python = "3.11"
"ubi:B1NARY-GR0UP/nwa" = "latest"

## grouped tasks

[tasks."check"]
depends = ["*:check"]

## common tasks

### check

[tasks."common:check"]
depends = ["common:check:*"]

[tasks."common:check:nwa"]
dir = "{{config_root}}"
run = [
    'find python -type f \( -name "*.py" -o -name "*.sh" \) -exec nwa check -i Apache-2.0 -c "© BeeAI a Series of LF Projects, LLC" {} \;',
    'find typescript -type d -name node_modules -prune -false -o \( -name "*.js" -o -name "*.ts" -o -name "*.sh" \) -exec nwa check -i Apache-2.0 -c "© BeeAI a Series of LF Projects, LLC" {} \;'
]
